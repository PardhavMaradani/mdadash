name: mdadash-test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # Package depends
  - fastapi
  - imdclient
  - ipykernel
  - MDAnalysis
  - python-socketio
  - uvicorn

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - httpx

  # Pip-only installs
  #- pip:
  #  - codecov
